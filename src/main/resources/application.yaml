server:
  port: 8080

spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    discovery:
      enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/cardValidator
    username : root
    password : root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect